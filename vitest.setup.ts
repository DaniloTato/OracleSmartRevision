import { configure } from '@testing-library/react'

configure({
    // getElementError: (message) => {
    //   const error = new Error(message || '')
    //   error.stack = ''
    //   return error
    // },
})
